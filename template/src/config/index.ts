export const PROJECT_NAME = 'Project';

export const REDUX_IDENTIFIER = `@@${PROJECT_NAME}/`;

export const BASE_URI = 'https://reqres.in';

export const BASE_URI_API = `${BASE_URI}/api/`;

export const API_URIS = {
  users: 'users',
}